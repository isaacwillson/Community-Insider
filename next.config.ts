import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Nothing to configure — this is a fully static prototype.
  // Vercel picks this up with zero config.
};

export default nextConfig;
